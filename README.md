# **minishell**

## **Project Title**

An attempt to create a small copycat of the original Bash with
reduced functionality. This shell is not POSIX-compatible. It
may have security issues, so do not run it as root

